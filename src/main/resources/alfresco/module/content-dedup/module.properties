module.id=org.alfresco.content-dedup
module.title=Content Dedup
module.description=Prevent duplicate file uploads by computing a SHA-256 hash of incoming content \
  and comparing it against existing nodes in the target folder (and optionally its ancestor \
  hierarchy) before the upload is persisted.
module.version=1.0.0
module.repo.version.min=26.1
